import logo from '../Assets/logo.png'

function Header() {

    return (
        <section>
            <div>
            <img src={logo} alt='logo entreprise'/>
            </div>
        </section>

    )

}

export default Header